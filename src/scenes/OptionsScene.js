import 'phaser';

export default class OptionsScene extends Phaser.Scene {
    constructor() {
        super('Options');
    }

    preload() {
        
    }

    create () {
        var logo = this.add.image(400, 300, 'placeholder');
        
    }


};